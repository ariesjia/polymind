/**
 * Site configuration - used for SEO meta tags, sitemap, robots.txt
 * Change SITE_URL when deploying to a different domain
 */
export const siteConfig = {
  /** Base URL with trailing slash, e.g. https://polymind-rho.vercel.app/ */
  SITE_URL: "https://polymind-rho.vercel.app/",
} as const;
